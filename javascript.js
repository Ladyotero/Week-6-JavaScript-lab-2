"use strict"

// Function to display band info on the webite
function displaybandList() {
 const banda = document.getElementById('banda');

banda.forEach(banda => {

// creating a list  for each banda

 const listItem=document.createElement('li');
  listItem.classList.add('banda');

// Create band details

const banddetails = \


});
}
